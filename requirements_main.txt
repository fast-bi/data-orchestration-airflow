# Main packages
apache-airflow-providers-google==17.1.0
apache-airflow-providers-airbyte==5.2.3
apache-airflow-providers-dbt-cloud==4.4.2
# Must match the cncf provider Airflow 2.11.2 ships (10.13.0): the bundled celery
# 3.15.2 CeleryKubernetesExecutor imports cncf's cli.definition module, absent in
# 10.3.0 — downgrading it broke `airflow celery worker`.
apache-airflow-providers-cncf-kubernetes==10.13.0
apache-airflow-providers-snowflake==6.5.2
connexion[swagger-ui]<3.0
cryptography==44.0.2
pyarrow==19.0.1
opentelemetry-exporter-otlp==1.30.0
opentelemetry-exporter-otlp-proto-common==1.30.0
opentelemetry-exporter-otlp-proto-http==1.30.0
opentelemetry-exporter-otlp-proto-grpc==1.30.0
opentelemetry-proto==1.30.0
# cncf-kubernetes 10.13.0 requires kubernetes>=35.0.0,<36.0.0 (matches 2.11.2 base).
kubernetes>=35.0.0,<36.0.0

# Security overrides: force patched transitive dependencies (Trivy CRITICAL remediation).
# Pulled in via apache-airflow-providers-google -> google-cloud-aiplatform[evaluation]
# and the bundled amazon redshift driver. pip check (run after install) validates resolution.
# Upper bound matches google-cloud-aiplatform[evaluation]'s litellm<1.86.0 cap so
# `pip check` stays green; 1.84.x/1.85.x already fix all three CVEs below.
litellm>=1.84.0,<1.86.0     # CVE-2026-35030, CVE-2026-42208, CVE-2026-49468
ray>=2.52.0                 # CVE-2025-62593
redshift-connector>=2.1.14  # CVE-2026-8838